import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'safety app reciever',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
